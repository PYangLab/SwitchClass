% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_heatmap.R
\name{plot_heatmap_quadrants}
\alias{plot_heatmap_quadrants}
\title{Plot heatmap for quadrant-classified features}
\usage{
plot_heatmap_quadrants(
  X,
  group,
  df_quad,
  cutoff = 0.15,
  palette_group = NULL,
  palette_quad = NULL,
  scale = TRUE,
  cap = 2,
  show_rownames = TRUE,
  show_colnames = TRUE,
  cluster_rows = FALSE,
  cluster_cols = FALSE
)
}
\arguments{
\item{X}{numeric matrix \link{features x samples}; expression matrix.}

\item{group}{factor/character of length = ncol(X); sample group labels.}

\item{df_quad}{data.frame with columns: \code{feature}, \code{quadrant}, and \code{delta}.}

\item{cutoff}{numeric; delta threshold for selecting strong quadrant features (default 0.15).
Quadrants Q1 and Q4 use \code{delta > cutoff}; Q2 and Q3 use \code{delta < -cutoff}.}

\item{palette_group}{optional named color vector for sample groups.}

\item{palette_quad}{optional named color vector for quadrants.}

\item{scale}{logical; z-score features (default TRUE).}

\item{cap}{numeric; cap scaled values at ±cap (default 2).}

\item{show_rownames, show_colnames}{logical; show labels (default TRUE).}

\item{cluster_rows, cluster_cols}{logical; cluster rows/columns (default FALSE).}

\item{width, height}{numeric; optional dimensions for PDF export.}
}
\value{
Invisibly returns the \code{pheatmap} object.
}
\description{
Generates a standardized heatmap of feature expression grouped by quadrant
(Q1–Q4) and sample group (e.g., HC / Pre / Post). Quadrants are optionally
filtered by delta cutoff thresholds.
}
\examples{
\dontrun{
plot_heatmap_quadrants_auto(
  X = crc.imputed,
  group = group,
  df_quad = df,
  cutoff = 0.15
)
}
}
